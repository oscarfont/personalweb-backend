/**
 * @author Óscar Font
 * =====================
 * LogLevel object enum
 * =====================
 * @description
 * Object that represents an enum which states the
 * different logging levels the logger used in the
 * application can have.
 */
export const LogLevel = {
    INFO: 0,
    DEBUG: 1,
    WARN: 2,
    ERROR: 3
}