/**
 * @author Óscar Font
 * ======================
 * MethodEnum object enum
 * ======================
 * @description
 * Object that represents an enum which states the
 * the possible HTTP verbs of the endpoints 
 * described in the backend server of the application.
 */
export const MethodEnum = {
    GET: 'get',
    POST: 'post',
    PUT: 'put',
    DELETE: 'delete'
} 